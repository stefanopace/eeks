{
	"type": "recursive",
	"set": [ { "name": "last" } ],
	"accumulator": "acc",
	"initial": "initial value",
	"execute": ["some command with {acc}"],
	"stop-condition": ["{acc}"],
	"resolvers": [ 
		{
			"type": "leaf",
			"execute": ["you stopped recursion!"]
		}
	]
}
