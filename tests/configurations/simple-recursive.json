{
	"type": "recursive",
	"set": [ { "name": "last" } ],
	"accumulator": { "name": "acc", "initial": "initial value" },
	"execute": ["some command with {acc}"],
	"stop-condition": ["{acc}"],
	"resolvers": [ 
		{
			"type": "leaf",
			"execute": ["you stopped recursion!"]
		}
	]
}
